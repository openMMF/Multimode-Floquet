===============================
openMMF library project
===============================

This project provides a generic algorithm to calculate the time evolution operator (and related quantities) for quantum systems with discrete spectrum, driven by an arbitrary set of harmonic fields.
