============
Installation
============

At the command line::

    $ pip install ShakenOL-Floquet

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ShakenOL-Floquet
    $ pip install ShakenOL-Floquet
